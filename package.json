{
  "name": "uketuner",
  "description": "uke tuner",
  "version": "0.0.1",
  "private": true,
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "4.x"
  },
  "engines": {
    "node": "0.10.x"
  }
}